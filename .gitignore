# Mac Crap
.DS_Store

.vscode

# Mail Subdirectories the user might want to change
mail/*

# exception to the rule
!mail/.gitkeep

# password file
config/dovecot.passwd
/config/dovecot.passwd

# TLS keys/certs — auto-generated, never commit
config/*.pem
